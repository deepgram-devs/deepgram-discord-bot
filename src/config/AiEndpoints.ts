/**
 * Use this object to set the endpoints for the AI requests.
 *
 * @property {string} response The endpoint to generate an answer to a help question.
 * @property {string} confirm The endpoint to confirm a generated response is helpful.
 * @property {string} deny The endpoint to mark a generated response as inaccurate.
 */
export const AiEndpoints = {
  response: "",
  confirm: "",
  deny: "",
};
